import ProfilePageConfig from './profile/ProfilePageConfig'
import ShopConfig from './shop-management/shopConfig'
import ErrorConfig from './errors/errorConfig'
import AuthConfig from './auth/authConfig'

const pagesConfig = [AuthConfig, ErrorConfig, ProfilePageConfig, ShopConfig]

export default pagesConfig
